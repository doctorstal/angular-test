import { ZoneRouterLinkDirective } from './zone-router-link.directive';

describe('ZoneRouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoneRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
